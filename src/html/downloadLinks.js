const fs = require('fs')
const { get } = require('http')
const { promisify } = require('util')
const {  } = require('node-canvas-api')
const getP = promisify(get)

const downloadLinks = ({ html, term, name, course }) => {
  const findLinks = 
}

module.exports = downloadLinks
